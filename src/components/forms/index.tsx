import { default as CameraForm } from "./Camera"
import { default as CatalogForm } from "./Catalog"
import { default as FlattReducForm } from "./FlattReduc"
import { default as OptionForm } from "./Option"
import { default as TelescopeForm } from "./Telescope"

const forms = {
  camera: CameraForm,
  catalog: CatalogForm,
  flattReduc: FlattReducForm,
  option: OptionForm,
  telescope: TelescopeForm,
}

export default forms
